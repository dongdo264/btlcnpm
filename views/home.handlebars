 <div class="container">
        <div class="col-sm-10">
            <div class="row">
                {{#each this.categories}}
                <div class="col-sm-3">
                <div class="card" style="width: 18rem;">
                <img src="/img/{{productID}}/view1.webp" class="card-img-top" alt="...">
            <div class="card-body">
                   <h5 class="card-title"></h5>
                 <p class="card-text">{{productName}} : {{CatPrice}}</p>
                <a href="/product/view/{{productID}}" class="btn btn-primary" title="Xem chi tiết">Xem chi tiết</a>
             </div>
            </div>
            </div>
            {{/each}}
            </div>
        </div>
        
</div>